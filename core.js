class AutoClicker { constructor(interval) { this.interval = interval; this.clicks = 0; this.isRunning = false; } start() { if (!this.isRunning) { this.isRunning = true; this.click(); } } stop() { this.isRunning = false; } click() { if (!this.isRunning) return; this.clicks++; console.log(`Click ${this.clicks}`); setTimeout(() => this.click(), this.interval); } } const clicker = new AutoClicker(1000); clicker.start(); setTimeout(() => { clicker.stop(); console.log(`Total clicks: ${clicker.clicks}`); }, 10000);